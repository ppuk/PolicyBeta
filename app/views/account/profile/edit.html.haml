%h2
  = t('.title')

= simple_form_for user, url: account_profile_path do |f|
  = f.input :username
  = f.input :email
  = f.input :password
  = f.button :submit, t('.submit'), class: 'btn btn-primary'

