= simple_form_for [:account, policy, evidence_item] do |f|
  = f.input :title
  = f.input :description, input_html: { rows: 10, data: { behavior: 'markdown' } }
  = f.button :submit, t('.submit'), class: 'btn btn-primary'
